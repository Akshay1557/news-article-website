<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login</title>
  <link rel="stylesheet" href="/css/users.css">
</head>
<body class="auth-page">
  <div class="auth-container">
    
    <!-- Close button -->
    <a href="/articles" class="close-btn">&times;</a>

    <h1 class="auth-title"> Please Login First  !</h1>

     <!-- Show error messages if any -->
    <% if (error && error.length > 0) { %>
      <div class="error-message"><%= error[0] %></div>
    <% } %>

    <form action="/users/login" method="POST" class="auth-form">
      <label for="email">Email Address</label>
      <input type="email" id="email" name="email" required>

      <label for="password">Password</label>
      <input type="password" id="password" name="password" required>

      <p>Forgot password ? <a href="/users/forgot-password">update password</a></p>
      <button type="submit" class="btn primary">🔑 Login</button>
    </form>

    <p class="auth-footer">
      Don’t have an account? <a href="/users/register">Register</a>
    </p>
  </div>
</body>
</html>
