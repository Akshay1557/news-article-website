<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Article</title>
  <link rel="stylesheet" href="/css/layout.css">
  <link rel="stylesheet" href="/css/articles/articleEdit.css">
</head>
<body>
  <%- include("../partials/header") %>

  <div class="article-form">
    <h1 class="form-title">✏️ Edit Article</h1>

    <form action="/articles/edit/<%= article._id %>" method="POST" enctype="multipart/form-data">
      
      <!-- Title -->
      <label for="title">Article Title</label>
      <input 
        type="text" 
        id="title" 
        name="title"  
        value="<%= article.title %>"  
        required>

      <!-- Content -->
      <label for="content">Content</label>
      <textarea id="content" name="content" required><%= article.content %></textarea>

      <!-- Current Image -->
      <% if (article.image) { %>
        <div class="form-group">
          <label>Current Image</label><br>
          <img src="<%= article.image %>" alt="Article image" class="preview-image">
        </div>
      <% } %>

      <!-- Upload new image -->
      <div class="form-group">
        <label for="image">Upload New Image</label>
        <input type="file" id="image" name="image" accept="image/*">
      </div>

      <!-- Submit -->
      <div class="form-actions">
        <button type="submit" class="btn primary">Update Article</button>
        <a href="/articles/<%= article._id %>" class="btn secondary">Cancel</a>
      </div>
    </form>
  </div>
</body>
</html>
